{
  "database": {
    "engine": "PostgreSQL",
    "version": "15+",
    "ssl": true,
    "maxConnections": 20,
    "idleTimeoutMillis": 30000,
    "connectionTimeoutMillis": 5000,
    "replication": "Multi-region fallback",
    "backupStrategy": "Daily snapshot + On-demand ZIP & GitHub sync"
  }
}
